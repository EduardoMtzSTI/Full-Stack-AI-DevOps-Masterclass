name: Demo CI Env Secrets

on:
  push
  # workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello from demo CI Env Secrets"

      - name: Install
        env:
          APP_ENV: demo_new
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        run: echo "Envioroment $APP_ENV using $DB_PASSWORD"
